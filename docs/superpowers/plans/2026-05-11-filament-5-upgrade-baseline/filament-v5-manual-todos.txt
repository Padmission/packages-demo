filament-v5 script run on 2026-05-12 — manual TODO findings: none.

The vendor/bin/filament-v5 Rector pass currently ships exactly one rule
(SimpleMethodChangesRector) that retypes the $action parameter on
getAuthorizationResponse / can / authorize when overridden on Resource
subclasses. No resources in this app override those methods, so the
rector pass produced zero file rewrites.

The script printed no TODO / MANUAL / WARNING / "cannot rewrite" / "not supported"
lines. The remaining v4 -> v5 breaking changes will be handled manually in
Phase 2 / Phase 3 of the upgrade plan.

Composer commands proposed by the script (deferred to Task 1.2, NOT run here):

    composer require filament/filament:"^5.0" -W --no-update
    composer require filament/spatie-laravel-media-library-plugin:"^5.0" -W --no-update
    composer require filament/spatie-laravel-settings-plugin:"^5.0" -W --no-update
    composer require filament/spatie-laravel-tags-plugin:"^5.0" -W --no-update
    composer require filament/custom-dashboards-plugin:"^5.0" -W --no-update
    composer remove filament/spatie-laravel-translatable-plugin -W --no-update
    composer require lara-zeus/spatie-translatable:"^2.0" -W --no-update
    composer update

Note: filament/spatie-laravel-translatable-plugin is being swapped for the
community fork lara-zeus/spatie-translatable. This app does not currently
require filament/spatie-laravel-translatable-plugin, so the swap may be
moot — verify in Task 1.2 before running those two lines.
